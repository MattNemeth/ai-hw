Python code so you'll need python installed

To run A Star Search:

python3 mjn_astar.py [input state list] [goal state list]

output will be printed to the screen

**Lists must use (',') and contain no spaces (' ')
